<!DOCTYPE html>
<html>
  <% include ../partials/head %>
  <body>
    <header>
      <nav class="top-nav">
        <div class="container">
          <div class="nav-wrapper">
            <a class="page-title">Billing Report</a>
          </div>
        </div>
      </nav>
      <style>
        /*
        html {
          background-image: url("/images/pattern.png");
        }
        */
        
        .top-nav {
          height: 84px;
        }
        
        nav {
          color: #fff;
          background-color: #ee6e73;
          width: 100%;
        }
        
        .page-title {
          font-size: 32px;
          line-height: 84px;
        }
      </style>
    </header>
    <div class="container">
      <div class="row">
        <div class="card">
          <div class="card-content">
            <span class="card-title">Summary</span>
            <p>You (the parent) currently have accumulated: <strong><%= sumParent %> miles</strong>.</p>
            <p>Your children have used: <strong><%= sumChild %> miles</strong>.</p>
            <br />
            <p>If you would like to use your own miles to pay for your children's, you can do that here.</p>
          </div>
          <div class="card-action">
            <% if (sumParent >= sumChild) {%>
              <a class="waves-effect waves-light btn" href="/redeemMiles/<%= trips[0].parentId._id.toString() %>">Redeem points</a>
            <% } else {%>
              <a class="disabled waves-effect waves-light btn">Redeem points</a>
            <% }%>
          </div>
        </div>
        <div class="card">
          <div class="card-content">
            <span class="card-title">Parent miles</span>
            <p>Parent miles gained from parent driving around</p>
            <table>
              <thead>
                <tr>
                  <th>Date</th>
                  <th>Miles</th>
                </tr>
              </thead>
              
              <tbody>
                <% for (var i = 0; i < trips.length; i++) {%>
                  <tr>
                    <td><%= trips[i].tripDate %></td>
                    <td><%= trips[i].totalDistance %></td>
                  </tr>
                <% }%>
              </tbody>
            </table>
          </div>
        </div>
        <div class="card">
          <div class="card-content">
            <span class="card-title">Student miles</span>
            <p>Student miles lost from use in transport</p>
            <table>
              <thead>
                <tr>
                  <th>Date</th>
                  <th>Miles used</th>
                </tr>
              </thead>
              
              <tbody>
                <% for (var i = 0; i < routes.length; i++) {%>
                  <tr>
                    <td><%= routes[i].tripId.tripDate %></td>
                    <td><%= routes[i].distance %></td>
                  </tr>
                <% }%>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </body>
  <% include ../partials/footer %>
</html>